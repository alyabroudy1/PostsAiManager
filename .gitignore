*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
# Any depth — the AI subsystem nests modules at core/ai/<name>/, which the old
# three-level patterns missed.
**/build/
captures/
.externalNativeBuild/
.cxx/
*.apk
*.aab

# Vendored llama.cpp (shallow clone, pinned to tag b10299).
# Becomes a git submodule once the Phase 7.2 spike confirms the revision.
core/ai/local/src/main/cpp/llama.cpp/

# Kotlin compiler session files (build noise)
.kotlin/
