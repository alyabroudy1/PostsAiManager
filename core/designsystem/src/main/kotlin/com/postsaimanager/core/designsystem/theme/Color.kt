package com.postsaimanager.core.designsystem.theme

import androidx.compose.ui.graphics.Color

/**
 * Posts AI Manager color palette.
 * Curated for a premium, professional feel with good contrast in both light/dark modes.
 */

// ── Primary ──
val PamBlue = Color(0xFF2563EB)          // Primary actions, selected state
val PamBlueLight = Color(0xFF60A5FA)     // Primary light variant
val PamBlueDark = Color(0xFF1D4ED8)      // Primary dark variant
val PamBlueContainer = Color(0xFFDBEAFE) // Primary container (light)
val PamBlueDarkContainer = Color(0xFF1E3A5F) // Primary container (dark)

// ── Secondary ──
val PamTeal = Color(0xFF0D9488)          // Secondary accent
val PamTealLight = Color(0xFF5EEAD4)
val PamTealContainer = Color(0xFFCCFBF1)
val PamTealDarkContainer = Color(0xFF134E4A)

// ── Tertiary ──
val PamAmber = Color(0xFFF59E0B)         // Warnings, pending states
val PamAmberContainer = Color(0xFFFEF3C7)
val PamAmberDarkContainer = Color(0xFF78350F)

// ── Error ──
val PamRed = Color(0xFFDC2626)           // Errors, destructive actions
val PamRedLight = Color(0xFFFCA5A5)
val PamRedContainer = Color(0xFFFEE2E2)
val PamRedDarkContainer = Color(0xFF7F1D1D)

// ── Success ──
val PamGreen = Color(0xFF16A34A)         // Success states
val PamGreenContainer = Color(0xFFDCFCE7)

// ── Neutral ──
val PamGray50 = Color(0xFFF9FAFB)
val PamGray100 = Color(0xFFF3F4F6)
val PamGray200 = Color(0xFFE5E7EB)
val PamGray300 = Color(0xFFD1D5DB)
val PamGray400 = Color(0xFF9CA3AF)
val PamGray500 = Color(0xFF6B7280)
val PamGray600 = Color(0xFF4B5563)
val PamGray700 = Color(0xFF374151)
val PamGray800 = Color(0xFF1F2937)
val PamGray900 = Color(0xFF111827)
val PamGray950 = Color(0xFF030712)

// ── Surface (Dark Mode) ──
val PamSurfaceDark = Color(0xFF0F172A)
val PamSurfaceContainerDark = Color(0xFF1E293B)
val PamSurfaceContainerHighDark = Color(0xFF334155)

// ── Tag colors ──
val PamTagColors = listOf(
    Color(0xFF3B82F6), // Blue
    Color(0xFF10B981), // Emerald
    Color(0xFFF59E0B), // Amber
    Color(0xFFEF4444), // Red
    Color(0xFF8B5CF6), // Violet
    Color(0xFFEC4899), // Pink
    Color(0xFF06B6D4), // Cyan
    Color(0xFFF97316), // Orange
)
